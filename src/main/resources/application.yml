server:
  port: 8080

spring:
  application:
    name: always-online-backend

  datasource:
    url: jdbc:mysql://localhost:3306/im?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: root
    password: zyzyqaz1
    driver-class-name: com.mysql.cj.jdbc.Driver

  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      timeout: 5000ms

mybatis-plus:
  mapper-locations: classpath*:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

jwt:
  secret: im-backend-secret-key-please-change
  access-token-expire-seconds: 7200

springdoc:
  swagger-ui:
    path: /swagger-ui.html